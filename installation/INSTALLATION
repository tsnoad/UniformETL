1. Create folders
mkdir logs
mkdir extract/extract_processes

1a. Config
ln -s /home/tsnoad/hotel/ /etc/uniformetl

cp config.template.php config.php
cp config.template.sh config.sh

nano config.php
nano config.sh

* * * * * su - tsnoad -c '/home/hotel/extract/extract_daemon.php >> /home/hotel/logs/extractdaemonlog'

* * * * * su - tsnoad -c '/home/hotel/transform/transform_daemon.php >> /home/hotel/logs/transformdaemonlog'

2. create database
createdb hotel
psql hotel < create.sql

3. create ssh key for data source server.
ssh-keygen -t rsa -f ~/.ssh/id_rsa_foxrep
cat ~/.ssh/id_rsa_foxrep.pub | ssh remote.server.com "cat >> ~/.ssh/authorized_keys2"

4. run extraction to get data from data source


5. run transformation to add data to database

